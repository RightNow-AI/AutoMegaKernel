"""Layer 3, Dynamism (continuous batching, dynamic shapes, MoE routing).

Placeholder package: the dynamism layer is on the roadmap (see ``program.md``) and is
exposed here so the installed ``automegakernel`` wheel reserves the import path. It has no
runtime modules yet; importing it must not pull in anything heavy.
"""
